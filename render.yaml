services:
  - type: web
    name: unifiedchat-audio-service
    runtime: docker
    plan: free
    rootDir: services/audio-service
    dockerfilePath: Dockerfile
    dockerContext: services/audio-service
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8084
