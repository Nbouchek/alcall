services:
  - type: web
    name: unifiedchat-janus-service
    runtime: docker
    region: oregon
    plan: starter
    

    autoDeploy: true
    numInstances: 1
# Note: Render will route HTTPS/WSS traffic to port 443 automatically
