services:
  - type: web
    name: alcall-janus-service
    runtime: docker
    region: frankfurt
    plan: starter

    autoDeploy: true
    numInstances: 1
# Note: Render will route HTTPS/WSS traffic to port 443 automatically
