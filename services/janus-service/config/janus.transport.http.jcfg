general: {
    enabled = true
    port = RENDER_PORT_PLACEHOLDER
    interface = "0.0.0.0"
    ip = "0.0.0.0"
    threads = 4
    http = true
    https = false

    # Additional HTTP settings
    base_path = "/janus"

    # CORS settings for web frontend
    cors: {
        enabled = true
        origin = "*"
        methods = "GET,POST,PUT,DELETE,OPTIONS,HEAD"
        headers = "Content-Type,Authorization"
    }

    # WebSocket settings
    ws = true
    ws_port = RENDER_WS_PORT_PLACEHOLDER
    ws_interface = "0.0.0.0"
    ws_ip = "0.0.0.0"
    # Redirect root to admin info
    redirect_root = "/admin/info"
}

admin: {
    admin_enabled = true
    admin_http = true
    admin_https = false
    admin_port = RENDER_PORT_PLACEHOLDER
    admin_interface = "0.0.0.0"
    admin_base_path = "/admin"
}
