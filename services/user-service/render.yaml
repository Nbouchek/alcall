services:
  - type: web
    name: alcall-user-service
    runtime: docker
    region: frankfurt
    plan: starter
    dockerfilePath: Dockerfile
    dockerContext: .

    healthCheckPath: /health
    autoDeploy: true
    numInstances: 1
