services:
  - type: web
    name: unifiedchat-user-service
    runtime: docker
    region: oregon
    plan: starter
    dockerfilePath: Dockerfile
    dockerContext: .
    
    healthCheckPath: /health
    autoDeploy: true
    numInstances: 1
