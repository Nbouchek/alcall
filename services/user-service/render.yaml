services:
  - type: web
    name: unifiedchat-user-service
    runtime: docker
    region: oregon
    plan: starter
    dockerfilePath: Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 8081
    healthCheckPath: /health
    autoDeploy: true
    numInstances: 1
