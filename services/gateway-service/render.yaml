services:
  - type: web
    name: unifiedchat-gateway-service
    runtime: docker
    region: oregon
    plan: starter
    buildCommand: docker build -t gateway-service .
    
    healthCheckPath: /health
    autoDeploy: true
    numInstances: 1
