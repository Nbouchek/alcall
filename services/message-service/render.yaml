services:
  - type: web
    name: unifiedchat-message-service
    runtime: docker
    region: oregon
    plan: starter
    dockerfilePath: Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 8083
    healthCheckPath: /health
    autoDeploy: true
    numInstances: 1
