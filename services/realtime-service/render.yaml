services:
  - type: web
    name: alcall-realtime-service
    runtime: docker
    region: frankfurt
    plan: starter
    buildCommand: docker build -t realtime-service .

    healthCheckPath: /health
    autoDeploy: true
    numInstances: 1
